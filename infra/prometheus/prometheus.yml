global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: sentinelai-gateway
    static_configs:
      - targets: ['gateway:3000']
    metrics_path: /metrics
